
@{
    ViewData["Title"] = "Index";
    List<Product> products = Model;
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body bg-dark font-weight-bold text-white">
                <p style="padding-bottom: 10px; text-align:center">
                    <a asp-action="Create" class="btn btn-outline-success" style="width: 160px">Create</a>
                </p>
                <h4 class="card-title text-white">Courses</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th class="text-white">Image</th>
                                <th class="text-white">Name</th>
                                <th class="text-white">Price</th>
                                <th class="text-white">Rate</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var product in products)
                            {
                                <tr>
                                    <td><img src="~/img/product/@product.Image" style="width: 100px !important; height:100px !important; object-fit: cover !important" /></td>
                                    <td class="text-white">@product.Name</td>
                                    <td class="text-white">@product.NewPice</td>
                                    <td class="text-white">@product.Rate</td>
                                    <td>
                                        <a class="btn btn-outline-info" asp-action="Detail" asp-route-id="@product.Id">Detail</a>
                                        <a class="btn btn-outline-primary" asp-action="Update" asp-route-id="@product.Id">Update</a>
                                        <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@product.Id">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <div>
                        <nav aria-label="..." id="pagination1">
                            <ul class="pagination pagination-lg">
                                @for (int i = 1; i <= ViewBag.PageCount; i++)
                                {
                                    if (ViewBag.Page == i)
                                    {
                                        <li class="page-item disabled">
                                            <a class="page-link" asp-action="Index" asp-route-page="@i">@i</a>
                                        </li>
                                    }
                                    else
                                    {
                                        <li class="page-item">
                                            <a class="page-link" asp-action="Index" asp-route-page="@i">@i</a>
                                        </li>
                                    }
                                }
                            </ul>
                        </nav>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>
<div class="row">
</div>


